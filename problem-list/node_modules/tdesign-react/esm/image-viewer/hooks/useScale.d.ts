import type { ImageScale } from '../type';
declare const useScale: (imageScale: ImageScale, visible: boolean) => {
    scale: number;
    onZoom: () => void;
    onZoomOut: () => void;
    onResetScale: () => void;
};
export default useScale;
